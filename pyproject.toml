[project]
name = "pytorch-for-deep-learning"
version = "0.1.0"
description = "Course materials for the DeepLearning.AI PyTorch for Deep Learning Professional Certificate"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.9",
    "torch>=2.11.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
